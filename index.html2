<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Links — South Pacific & PNW</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;1,400&family=Inter:wght@300;400;500&display=swap');

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --ink:     #0E1A15;
    --deep:    #0F3D2C;
    --teal:    #0F6E56;
    --sea:     #1D9E75;
    --foam:    #9FE1CB;
    --pale:    #E8F5F0;
    --sand:    #F5F0E8;
    --salt:    #FAFAF8;
    --mist:    #6B8A7E;
    --gold:    #C49A3C;
    --rust:    #8B4A2A;

    --ff-display: 'Playfair Display', Georgia, serif;
    --ff-body:    'Inter', system-ui, sans-serif;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: var(--ff-body);
    background: var(--salt);
    color: var(--ink);
    min-height: 100vh;
    position: relative;
    overflow-x: hidden;
  }

  /* ── HORIZON BACKGROUND ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background:
      radial-gradient(ellipse 120% 60% at 50% -10%, rgba(15,110,86,0.12) 0%, transparent 70%),
      radial-gradient(ellipse 80% 40% at 80% 110%, rgba(29,158,117,0.08) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
  }

  /* ── LAYOUT ── */
  .page {
    position: relative;
    z-index: 1;
    max-width: 480px;
    margin: 0 auto;
    padding: 0 20px 80px;
  }

  /* ── HEADER ── */
  .header {
    padding: 48px 0 32px;
    text-align: center;
  }

  .avatar-ring {
    width: 80px; height: 80px;
    border-radius: 50%;
    border: 2px solid var(--foam);
    margin: 0 auto 20px;
    display: flex; align-items: center; justify-content: center;
    background: linear-gradient(135deg, var(--deep) 0%, var(--sea) 100%);
    font-size: 28px;
    color: var(--foam);
    font-family: var(--ff-display);
    font-weight: 600;
    letter-spacing: -1px;
    position: relative;
  }

  .avatar-ring::after {
    content: '';
    position: absolute;
    inset: -5px;
    border-radius: 50%;
    border: 1px solid rgba(159,225,203,0.3);
  }

  .handle {
    font-family: var(--ff-body);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--mist);
    margin-bottom: 8px;
  }

  .name {
    font-family: var(--ff-display);
    font-size: 26px;
    font-weight: 600;
    color: var(--ink);
    line-height: 1.2;
    margin-bottom: 12px;
  }

  .bio {
    font-size: 14px;
    color: var(--mist);
    line-height: 1.65;
    font-weight: 300;
    max-width: 320px;
    margin: 0 auto 16px;
  }

  /* ── VOYAGE BANNER ── */
  .voyage-banner {
    background: linear-gradient(135deg, var(--deep) 0%, var(--teal) 60%, var(--sea) 100%);
    border-radius: 14px;
    padding: 16px 20px;
    margin-bottom: 32px;
    display: flex;
    align-items: center;
    gap: 14px;
    position: relative;
    overflow: hidden;
  }

  .voyage-banner::before {
    content: '';
    position: absolute;
    top: -20px; right: -20px;
    width: 120px; height: 120px;
    border-radius: 50%;
    background: rgba(255,255,255,0.04);
  }

  .voyage-dot {
    width: 10px; height: 10px;
    border-radius: 50%;
    background: var(--foam);
    box-shadow: 0 0 0 3px rgba(159,225,203,0.3);
    animation: pulse 2.2s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes pulse {
    0%, 100% { box-shadow: 0 0 0 3px rgba(159,225,203,0.3); }
    50%       { box-shadow: 0 0 0 6px rgba(159,225,203,0.1); }
  }

  .voyage-text { flex: 1; }
  .voyage-label {
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--foam);
    margin-bottom: 2px;
  }
  .voyage-title {
    font-family: var(--ff-display);
    font-size: 15px;
    color: #fff;
    font-style: italic;
  }
  .voyage-sub {
    font-size: 11px;
    color: rgba(255,255,255,0.55);
    margin-top: 1px;
  }

  /* ── SECTION LABELS ── */
  .section-label {
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--mist);
    margin-bottom: 12px;
    margin-top: 28px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .section-label::after {
    content: '';
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, rgba(107,138,126,0.2), transparent);
  }

  /* ── LINK CARDS ── */
  .card {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 15px 16px;
    border-radius: 13px;
    margin-bottom: 10px;
    background: #fff;
    border: 1px solid rgba(15,110,86,0.1);
    text-decoration: none;
    color: inherit;
    transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(15,110,86,0.12);
    border-color: rgba(15,110,86,0.25);
  }

  .card:active { transform: translateY(0); }

  .card-icon {
    width: 42px; height: 42px;
    border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
    flex-shrink: 0;
  }

  .card-text { flex: 1; min-width: 0; }
  .card-title {
    font-size: 14px;
    font-weight: 500;
    color: var(--ink);
    margin-bottom: 2px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .card-desc {
    font-size: 12px;
    color: var(--mist);
    font-weight: 300;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .card-arrow {
    font-size: 14px;
    color: var(--foam);
    flex-shrink: 0;
    font-weight: 300;
  }

  /* Card color variants */
  .card-teal   .card-icon { background: var(--pale); color: var(--teal); }
  .card-deep   .card-icon { background: #E8F0EC; color: var(--deep); }
  .card-gold   .card-icon { background: #F5EED8; color: var(--gold); }
  .card-rust   .card-icon { background: #F5EAE4; color: var(--rust); }
  .card-mist   .card-icon { background: #EFF2F1; color: var(--mist); }
  .card-ink    .card-icon { background: #E8ECEA; color: var(--ink); }

  /* Coming-soon state */
  .card.locked {
    opacity: 0.5;
    pointer-events: none;
    filter: grayscale(0.4);
  }

  .card.locked::after {
    content: 'After voyage';
    position: absolute;
    top: 8px; right: 10px;
    font-size: 9px;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--mist);
    background: var(--pale);
    padding: 2px 7px;
    border-radius: 20px;
  }

  /* ── FEATURED CARD (newsletter) ── */
  .card-featured {
    background: linear-gradient(135deg, var(--deep) 0%, var(--teal) 100%);
    border-color: transparent;
    color: #fff;
  }

  .card-featured .card-title { color: #fff; }
  .card-featured .card-desc  { color: rgba(255,255,255,0.6); }
  .card-featured .card-arrow { color: var(--foam); }
  .card-featured .card-icon  { background: rgba(255,255,255,0.12); color: var(--foam); }

  .card-featured:hover {
    border-color: transparent;
    box-shadow: 0 10px 28px rgba(15,110,86,0.28);
  }

  /* ── AFFILIATE ROW (small chips) ── */
  .affiliate-row {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 4px;
  }

  .aff-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    border-radius: 10px;
    background: #fff;
    border: 1px solid rgba(15,110,86,0.1);
    text-decoration: none;
    font-size: 12px;
    font-weight: 400;
    color: var(--ink);
    flex: 1;
    min-width: 120px;
    transition: transform 0.15s ease, border-color 0.15s ease;
  }

  .aff-chip:hover {
    transform: translateY(-1px);
    border-color: rgba(15,110,86,0.25);
  }

  .aff-chip-icon { font-size: 14px; }
  .aff-chip-text { font-size: 11px; color: var(--mist); }

  /* ── COMING SOON SECTION ── */
  .coming-soon-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
  }

  .cs-card {
    background: #fff;
    border: 1px dashed rgba(107,138,126,0.25);
    border-radius: 12px;
    padding: 14px;
    opacity: 0.6;
  }

  .cs-icon { font-size: 20px; margin-bottom: 6px; }
  .cs-title { font-size: 12px; font-weight: 500; color: var(--ink); margin-bottom: 2px; }
  .cs-when  { font-size: 10px; color: var(--mist); letter-spacing: 0.04em; }

  /* ── VOYAGE PROGRESS ── */
  .voyage-progress {
    background: #fff;
    border: 1px solid rgba(15,110,86,0.1);
    border-radius: 13px;
    padding: 16px;
    margin-top: 4px;
  }

  .progress-label {
    font-size: 11px;
    color: var(--mist);
    margin-bottom: 8px;
    display: flex;
    justify-content: space-between;
  }

  .progress-bar {
    height: 4px;
    background: var(--pale);
    border-radius: 4px;
    overflow: hidden;
    margin-bottom: 12px;
  }

  .progress-fill {
    height: 100%;
    border-radius: 4px;
    background: linear-gradient(to right, var(--teal), var(--foam));
    width: 0%;
    transition: width 1s ease;
  }

  .progress-stops {
    display: flex;
    justify-content: space-between;
  }

  .stop {
    text-align: center;
    flex: 1;
  }

  .stop-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--pale);
    border: 1.5px solid var(--foam);
    margin: 0 auto 3px;
  }

  .stop-dot.done   { background: var(--teal); border-color: var(--teal); }
  .stop-dot.active { background: var(--foam); border-color: var(--sea); box-shadow: 0 0 0 3px rgba(159,225,203,0.3); }

  .stop-name { font-size: 9px; color: var(--mist); }

  /* ── FOOTER ── */
  .footer {
    text-align: center;
    padding-top: 32px;
    font-size: 11px;
    color: var(--mist);
    opacity: 0.5;
    font-weight: 300;
  }

  /* ── SCROLL REVEAL ── */
  .reveal {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.5s ease, transform 0.5s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: none;
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal { opacity: 1; transform: none; }
    @keyframes pulse { from, to { box-shadow: 0 0 0 3px rgba(159,225,203,0.3); } }
  }

  @media (max-width: 400px) {
    .coming-soon-grid { grid-template-columns: 1fr; }
  }
</style>
</head>
<body>
<div class="page">

  <!-- HEADER -->
  <header class="header reveal">
    <div class="avatar-ring">SP</div>
    <div class="handle">@ugh.itsandrew</div>
    <div class="name">Sailing South Pacific</div>
    <p class="bio">Palau → Raja Ampat → Komodo → Bali · Jul–Aug 2026 · Travel, adventure, Pacific</p>
  </header>

  <!-- VOYAGE BANNER -->
  <div class="voyage-banner reveal">
    <div class="voyage-dot"></div>
    <div class="voyage-text">
      <div class="voyage-label">Currently aboard S/Y Argo</div>
      <div class="voyage-title">40-Day South Pacific Voyage</div>
      <div class="voyage-sub">Follow the daily dispatch → Day X of sailing</div>
    </div>
    <div style="font-size:22px;">⛵</div>
  </div>

  <!-- VOYAGE PROGRESS -->
  <div class="voyage-progress reveal">
    <div class="progress-label">
      <span>Route progress</span>
      <span style="color:var(--teal);font-weight:500;">Jul 14 → Aug 22</span>
    </div>
    <div class="progress-bar">
      <div class="progress-fill" id="progressFill"></div>
    </div>
    <div class="progress-stops">
      <div class="stop">
        <div class="stop-dot active" id="dot-palau"></div>
        <div class="stop-name">Palau</div>
      </div>
      <div class="stop">
        <div class="stop-dot" id="dot-raja"></div>
        <div class="stop-name">Raja Ampat</div>
      </div>
      <div class="stop">
        <div class="stop-dot" id="dot-komodo"></div>
        <div class="stop-name">Komodo</div>
      </div>
      <div class="stop">
        <div class="stop-dot" id="dot-bali"></div>
        <div class="stop-name">Bali</div>
      </div>
    </div>
  </div>

  <!-- SECTION: FOLLOW THE STORY -->
  <div class="section-label reveal">Follow the story</div>

  <a href="#" class="card card-teal reveal" onclick="return false">
    <div class="card-icon">📬</div>
    <div class="card-text">
      <div class="card-title">The Voyage Log — Newsletter</div>
      <div class="card-desc">Daily journal entries, unfiltered. Free to start.</div>
    </div>
    <div class="card-arrow">→</div>
  </a>

  <a href="#" class="card card-featured reveal" onclick="return false">
    <div class="card-icon">🌊</div>
    <div class="card-text">
      <div class="card-title">Follow the voyage — subscribe</div>
      <div class="card-desc">Get dispatches while I'm at sea</div>
    </div>
    <div class="card-arrow">→</div>
  </a>

  <!-- SECTION: PLAN YOUR TRIP -->
  <div class="section-label reveal">Plan your trip</div>

  <a href="#" class="card card-deep reveal" onclick="return false">
    <div class="card-icon">🗺️</div>
    <div class="card-text">
      <div class="card-title">Oregon Coast Guide — PDF</div>
      <div class="card-desc">Haystack Rock, Thor's Well, hidden stops on 101</div>
    </div>
    <div class="card-arrow">→</div>
  </a>

  <a href="#" class="card card-gold reveal" onclick="return false">
    <div class="card-icon">⛵</div>
    <div class="card-text">
      <div class="card-title">How I Got on a Sailboat — Guide</div>
      <div class="card-desc">Seamester, crew agencies, costs, what to pack</div>
    </div>
    <div class="card-arrow">→</div>
  </a>

  <a href="#" class="card card-mist reveal" onclick="return false">
    <div class="card-icon">📞</div>
    <div class="card-text">
      <div class="card-title">Book a Trip Planning Call</div>
      <div class="card-desc">60 min · Pacific Northwest · South Pacific</div>
    </div>
    <div class="card-arrow">→</div>
  </a>

  <!-- SECTION: GEAR I TRUST -->
  <div class="section-label reveal">Gear I actually use</div>

  <div class="reveal">
    <div class="affiliate-row">
      <a href="https://www.rei.com" class="aff-chip" target="_blank" rel="noopener">
        <span class="aff-chip-icon">🏕️</span>
        <div>
          <div style="font-weight:500;">REI</div>
          <div class="aff-chip-text">Outdoor gear</div>
        </div>
      </a>
      <a href="https://www.worldnomads.com" class="aff-chip" target="_blank" rel="noopener">
        <span class="aff-chip-icon">🛡️</span>
        <div>
          <div style="font-weight:500;">World Nomads</div>
          <div class="aff-chip-text">Adventure insurance</div>
        </div>
      </a>
    </div>
    <div class="affiliate-row" style="margin-top:8px;">
      <a href="https://gopro.com" class="aff-chip" target="_blank" rel="noopener">
        <span class="aff-chip-icon">📷</span>
        <div>
          <div style="font-weight:500;">GoPro</div>
          <div class="aff-chip-text">Camera + housing</div>
        </div>
      </a>
      <a href="https://www.dan.org" class="aff-chip" target="_blank" rel="noopener">
        <span class="aff-chip-icon">🤿</span>
        <div>
          <div style="font-weight:500;">DAN</div>
          <div class="aff-chip-text">Dive insurance</div>
        </div>
      </a>
    </div>
  </div>

  <!-- SECTION: COMING AFTER VOYAGE -->
  <div class="section-label reveal">Coming after the voyage</div>

  <div class="coming-soon-grid reveal">
    <div class="cs-card">
      <div class="cs-icon">🐠</div>
      <div class="cs-title">Dive Trip Planning Guide</div>
      <div class="cs-when">Available · Sep 2026</div>
    </div>
    <div class="cs-card">
      <div class="cs-icon">🖼️</div>
      <div class="cs-title">Photo Print Shop</div>
      <div class="cs-when">Manta rays · Pink Beach · Komodo</div>
    </div>
    <div class="cs-card">
      <div class="cs-icon">🎥</div>
      <div class="cs-title">Voyage Documentary</div>
      <div class="cs-when">YouTube · Fall 2026</div>
    </div>
    <div class="cs-card">
      <div class="cs-icon">🎓</div>
      <div class="cs-title">Creator Workshop</div>
      <div class="cs-when">Travel content systems</div>
    </div>
  </div>

  <!-- FOOTER -->
  <div class="footer reveal">
    <p>Building in public · Oregon &amp; the South Pacific · 2026</p>
  </div>

</div>

<script>
// ── SCROLL REVEAL ──────────────────────────────────────────────────────────
const reveals = document.querySelectorAll('.reveal');
const observer = new IntersectionObserver(entries => {
  entries.forEach((e, i) => {
    if (e.isIntersecting) {
      setTimeout(() => e.target.classList.add('visible'), i * 60);
      observer.unobserve(e.target);
    }
  });
}, { threshold: 0.08, rootMargin: '0px 0px -30px 0px' });
reveals.forEach(el => observer.observe(el));

// ── VOYAGE PROGRESS BAR ────────────────────────────────────────────────────
(function() {
  const start  = new Date('2026-07-14');
  const end    = new Date('2026-08-22');
  const rajaStart   = new Date('2026-07-22');
  const komodoStart = new Date('2026-08-06');
  const baliStart   = new Date('2026-08-13');

  const now    = new Date();
  const total  = end - start;
  const elapsed = Math.max(0, Math.min(now - start, total));
  const pct    = total > 0 ? (elapsed / total) * 100 : 0;

  setTimeout(() => {
    document.getElementById('progressFill').style.width = pct + '%';
  }, 600);

  function setDot(id, state) {
    const el = document.getElementById(id);
    if (!el) return;
    el.className = 'stop-dot' + (state === 'done' ? ' done' : state === 'active' ? ' active' : '');
  }

  if (now < start) {
    setDot('dot-palau', '');
  } else if (now < rajaStart) {
    setDot('dot-palau', 'active');
  } else if (now < komodoStart) {
    setDot('dot-palau', 'done');
    setDot('dot-raja', 'active');
  } else if (now < baliStart) {
    setDot('dot-palau', 'done');
    setDot('dot-raja', 'done');
    setDot('dot-komodo', 'active');
  } else if (now <= end) {
    setDot('dot-palau', 'done');
    setDot('dot-raja', 'done');
    setDot('dot-komodo', 'done');
    setDot('dot-bali', 'active');
  } else {
    setDot('dot-palau', 'done');
    setDot('dot-raja', 'done');
    setDot('dot-komodo', 'done');
    setDot('dot-bali', 'done');
  }
})();
</script>
</body>
</html>
